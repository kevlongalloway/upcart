# The landing page is a static HTML/CSS site with no build step, so it has no
# environment variables. The provisioning API URL is defined inline in
# `index.html` as `PROVISION_URL`.
#
# To point the landing page at a different provisioning service, edit:
#   landing/index.html  →  const PROVISION_URL = 'https://provision.upcart.online';
