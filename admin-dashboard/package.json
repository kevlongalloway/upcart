{
  "name": "upcart-dashboard",
  "version": "1.0.0",
  "description": "Upcart merchant admin dashboard — static SPA deployed on Cloudflare Pages.",
  "private": true,
  "scripts": {
    "dev": "npx http-server public -p 3000 -c-1",
    "build": "echo 'No build step — public/ is deployed as-is.'"
  }
}
